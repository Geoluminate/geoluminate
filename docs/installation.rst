============
Installation
============

At the command line::

    $ easy_install geoluminate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv geoluminate
    $ pip install geoluminate
